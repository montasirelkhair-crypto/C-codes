#include <stdio.h>

int main()
{
    double price[] = {5.0, 10.0, 12.0, 15.0}; //array

    char name[] = "Bro"; //string is an array of characters

    printf("%lf", price[0]); //select index number

    return 0;
}