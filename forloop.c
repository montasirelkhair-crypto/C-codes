#include <stdio.h>

int main() {
    // for loop = repeats a section of code a limited amount of times

    for(int i = 1; i <= 10; i++) { //or even i+= for example
        //printf("test\n"); //test is printed 10 times. 1 for each ideration.  
        printf("%d\n", i); //counts from 1 to 10
    }

    return 0;
}