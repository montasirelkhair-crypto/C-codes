#include <stdio.h>
#include <math.h> //includes a lot of math functions
 
int main () {

    double A = sqrt(9);
    double B = pow(2, 4); //2 to power of 4
    int C = round(3.14); //rounded automatically = 3
    int D = ceil(3.14); //rounded up = 4
    int E = floor(3.99); //rounded down = 3
    double F = fabs(-100); //make - number to + number = 100
    double G = log(3); //logarithmic of a number
    double H = sin(2); //sin of a no
    double I = cos(45);
    double J = tan(45);

    printf("\n%d", D);
 }